name: "🚀 publish"

on:
  push:
    branches:
      - master

jobs:
  publish-gpr:
    name: 🚀 publish
    runs-on: ubuntu-latest
    steps:
      - name: 📚 checkout
        uses: actions/checkout@v2
      - name: 🟢 node
        uses: actions/setup-node@v2
        with:
          node-version: 12
          registry-url: https://npm.pkg.github.com/
          scope: "@ferch01992"
      - name: 🚀 install
        run: npm install
      - name: 🔨 build
        run: npm run build
      - name: 🚀 publish
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
          NPM_TOKEN: ${{secrets.GIT_EXAM_TOKEN}}
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
